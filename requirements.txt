# Reproducibility — versions used to generate the published figures.
# Tested on Python 3.13 (macOS arm64). Loose lower bounds; upper bounds
# only where a specific minor version was relied on.
matplotlib>=3.5,<4
numpy>=1.21,<3
scipy>=1.7,<2
requests>=2.28,<3
